<Window x:Class="Superheroes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Superheroes"
        mc:Ignorable="d"
        Title="Superheroes_Triggers" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="nombrePersonaje">

            <Setter Property="Foreground" Value="White"></Setter>
            <Setter Property="FontFamily" Value="Impact"></Setter>
            <Setter Property="FontSize" Value="36"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
            <Setter Property="TextWrapping" Value="Wrap"></Setter>
            <Setter Property="TextAlignment" Value="Center"></Setter>
            <Setter Property="VerticalAlignment" Value="Top"></Setter>
            <Setter Property="HorizontalAlignment" Value="Left"></Setter>
            <Setter Property="Margin" Value="10,10,0,0"></Setter>
            <Setter Property="DockPanel.Dock" Value="Left"/>

            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect />
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform CenterX="0" CenterY="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseLeftButtonDown" >
                    <BeginStoryboard Name="RotateOnHover">
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                            From="0" To="360"
                            Duration="0:0:2"
                            AutoReverse="False" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseRightButtonDown" >
                    <StopStoryboard BeginStoryboardName="RotateOnHover"/>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Image" x:Key="logos1">
            <Setter Property="Height" Value="45"></Setter>
            <Setter Property="DockPanel.Dock" Value="Right"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Vengador}" Value="True">
                    <Setter Property="Visibility" Value="Visible"></Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Image" x:Key="logos2">
            <Setter Property="Height" Value="45"></Setter>
            <Setter Property="DockPanel.Dock" Value="Right"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Xmen}" Value="True">
                    <Setter Property="Visibility" Value="Visible"></Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Image" x:Key="FlechaRotao">

            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="180"/>
                </Setter.Value>
            </Setter>

        </Style>

        <Style TargetType="Grid" x:Key="Fondo">
            <Setter Property="Background" Value="IndianRed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Heroe}" Value="True">
                    <Setter Property="Background" Value="Aquamarine" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="ImagenFondo">
            <Setter Property="Opacity" Value="0.5"/>
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="DarkViolet" />
                    <Setter Property="BorderThickness" Value="5" />
                    <Setter Property="Padding" Value="5" />
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Style="{StaticResource Fondo}">
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0, 0, 10, 0">

            <TextBlock Style="{StaticResource nombrePersonaje}" Text="{Binding Nombre}"/>

            <Image Style="{StaticResource logos1}" Source="Assets/avengers.png"/>

            <Image Style="{StaticResource logos2}" Source="Assets/xmen.png"/>

        </DockPanel>

        <Grid Grid.Row="1" Margin="10">
            <Border Style="{StaticResource ImagenFondo}">
                <Image Source="{Binding ImagenRuta}" x:Name="ImagenFondo"
                   Stretch="Fill"
                   VerticalAlignment="Stretch"
                   HorizontalAlignment="Stretch" />
            </Border>
        </Grid>

        <DockPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="Assets/arrow.png" Height="25" Margin="0,0,5,0" MouseLeftButtonDown="ClickAnterior"/>
            <TextBlock x:Name="ContadorTextBlock" FontSize="20" FontWeight="Bold" Text="1/3"/>
            <Image Style="{StaticResource FlechaRotao}" Source="Assets/arrow.png" MouseLeftButtonDown="ClickSiguiente"/>
        </DockPanel>
    </Grid>
</Window>
